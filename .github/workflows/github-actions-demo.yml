name: GitHub Actions Demo
on:
  push:
    branches:
      - main
      - master
jobs:
  Explore-GitHub-Actions:
    runs-on: ubuntu-latest
    steps:
      - name: Check out repository code
        uses: actions/checkout@v2
      - name: List files in the repository
        run: |
          ls ${{ github.workspace }}
          curl -JLO -H "Accept: application/vnd.github+json" -H "Authorization: Bearer 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" https://api.github.com/repos/alexvatti/hello-world-lib/actions/artifacts/362978694/zip
          unzip lib-artifact.zip 
          gcc hello.c -Lladd -o hello
          ./hello
      - run: echo "This job's status is ${{ job.status }}."
      
      - name : Artifacts
        uses: actions/upload-artifact@v3
        with:
          name: my-artifact
          path: hello
